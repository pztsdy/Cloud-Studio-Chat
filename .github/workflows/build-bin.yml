name: Build and Push Binaries

on:
  push:
    branches:
      - main
      - test
jobs:
  build-windows:
    runs-on: windows-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        
      - name: Set up MinGW
        uses: egor-tensin/setup-mingw@v2
        with:
          platform: x64

      - name: Compile server.cpp for Windows
        run: |
          mkdir -p bin\windows
          g++ server.cpp -o bin\windows\server.exe -mconsole -lws2_32 -static

      - name: Compile client.cpp for Windows
        run: |
          g++ client.cpp -o bin\windows\client.exe -mwindows -lws2_32 -static

      - name: Upload Windows binaries
        uses: actions/upload-artifact@v4
        with:
          name: windows-binaries
          path: bin/windows/

  build-linux:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Compile server.cpp for Linux
        run: |
          mkdir -p bin/linux
          g++ server.cpp -o bin/linux/server -pthread

      - name: Upload Linux binaries
        uses: actions/upload-artifact@v4
        with:
          name: linux-binaries
          path: bin/linux/

  push-binaries:
    needs: [build-windows, build-linux]
    runs-on: ubuntu-latest
    if: success()
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          
      - name: Download Windows binaries
        uses: actions/download-artifact@v4
        with:
          name: windows-binaries
          path: bin/windows/

      - name: Download Linux binaries
        uses: actions/download-artifact@v4
        with:
          name: linux-binaries
          path: bin/linux/

      - name: Configure Git
        run: |
          git config user.name "github-actions"
          git config user.email "github-actions@users.noreply.github.com"

      - name: Add and commit binaries
        run: |
          git add bin/windows/server.exe
          git add bin/windows/client.exe
          git add bin/linux/server
          git commit -m "[skip ci] Add compiled binaries" || echo "No changes to commit"

      - name: Push changes
        run: git push